{
  "run": "phase-100620",
  "phase": "100620",
  "findings": [
    {
      "id": "F-01",
      "severity": "medium",
      "title": "Definition of Done box \"Required approval is obtained\" was checked, and Final Status was set to PASS, while approval has not happened and no verifier is recorded",
      "evidence": "private/clio-private/roadmap/phase-100620-retrieval-stage-scores.md, staged diff of the nested private repo: the developer rewrote `- [ ] Required approval is obtained.` to `- [x] Required approval is obtained (downstream pipeline step).` and added the text \"(downstream pipeline step)\" to make the box true. In the same staged edit the Attribution table still reads `| Adversary | r1 | [TBD] | [TBD] |`, `| Remediator | r1 | [TBD] | [TBD] |`, `| Remedy Approver | r1 | [TBD] | [TBD] |`, `| Finalize | r1 | [TBD] | [TBD] |`, and section 12 still reads `- Verifier: [TBD]`. `### Final Status` was changed from the template `PASS | PASS WITH DOCUMENTED LIMITATIONS | BLOCKED | FAILED` to `PASS`.",
      "requirement_ref": "phase doc section 9 \"Definition of Done\"; phase doc section 12 \"Verification Sign-Off\"; AGENTS.md \"Final Verification\" (state clearly what was verified)",
      "recommendation": "Leave the approval box unchecked until the remedy approver and finalizer sign, and do not fix `Final Status` to PASS while `Verifier:` is still `[TBD]`. Either mark the box as pending with a pointer to the downstream step, or set Final Status only after a verifier row exists.",
      "status": "resolved",
      "resolution": "RESOLVED by withdrawing the unsupported claim. The Definition-of-Done bullet is unchecked again and now reads: \"- [ ] Required approval is obtained \u2014 **not claimed in remediation r1**: no verifier and no approver have run, so this box stays unchecked and \u00a712 `Final Status` is left undecided until the Remedy Approver signs.\" \u00a712 Final Status now reads: \"Not decided. The implementer's `PASS` was withdrawn in remediation r1: `Verifier:` was still `[TBD]`, so no independent verification had happened, and the DoD approval box above is unchecked. The Remedy Approver records one of `PASS | PASS WITH DOCUMENTED LIMITATIONS | BLOCKED | FAILED` here.\" The `Verifier: [TBD]` and `Human Approver: [TBD]` rows are untouched, so no verifier and no approval are claimed anywhere in the file, and the attribution row records only 'Remediator r1 | Command Code (DeepSeek V4 Flash (latest) Max) | done'."
    },
    {
      "id": "F-02",
      "severity": "medium",
      "title": "AC-100620-03 required \"Before/after test output\" but the phase file supplies only post-change tests; the pre-change fused score was never captured",
      "evidence": "phase doc section 9 table row AC-100620-03: `Required Evidence | Before/after test output`. The `Evidence (actual, 2026-09-26)` row for AC-100620-03 cites only post-change tests: `reranker_keeps_scores_and_existing_order_behavior`, `identical_inputs_produce_identical_scores_and_ranks` (crates/clio-retrieve/src/stage_score_tests.rs:262, :300) and `raw_scores_are_recorded_per_list_and_never_change_the_fused_score` (crates/clio-retrieve/src/fusion_tests.rs:73). None of these compares against the pre-change code; `identical_inputs_produce_identical_scores_and_ranks` runs the post-change binary twice. The only pre-change artifact in the run log is a coverage JSON (`/tmp/cov-baseline.json`), not a score snapshot. Task 1 Required Change 4 asks for the fused `score` to stay \"byte-for-byte equal to the pre-change value\".",
      "requirement_ref": "phase doc section 9 AC-100620-03; section 5 Task 1 Required Changes item 4; section 10 \"Verification Rule\"",
      "recommendation": "I verified the claim independently and it holds: building HEAD and the staged tree, then running both `clio recall --output json` binaries against one shared SQLite store, gives bit-identical top-level `score` values (0.01182377049180328, 0.011609345351043642, 0.011448412545316509) and identical ordering and ranks, with the per-hit `scores` object as the only payload difference. Add that before/after output to section 9 as the AC-100620-03 evidence, or state plainly that only post-change determinism plus code inspection was done. The harness is kept at private/clio-private/runs/phase-100620/adversary-compare-pre-post.py and its output at private/clio-private/runs/phase-100620/adversary-pre-post-compare.json.",
      "status": "resolved",
      "resolution": "RESOLVED by adding the real before/after output the criterion required. I reproduced the comparison myself with a copy of the adversary harness (private/clio-private/runs/phase-100620/remediator-compare-pre-post.py, output remediator-pre-post-compare.json), run under `timeout 900`: exit 0, `failures: []`. The pre-change binary built from HEAD `9a0876c` (`~/adv-scratch/pre/target/debug/clio`) and the post-change `target/debug/clio` were both run against ONE freshly seeded SQLite store whose item timestamps are in the future, so the recency term is exactly 1.0 and the fused score is bit-reproducible: `pre_scores == post_scores == [0.01182377049180328, 0.011609345351043642, 0.011448412698412699]`, identical hit order and identical `dense_rank` / `lexical_rank` / `consolidated` / `dedup`, `pre_has_scores_object=false`, and the new `scores` object is the only payload difference (`\"final\":0.01182377049180328,\"keyword\":2.4821624004032445,\"reranker\":null,\"semantic\":null`). The two further scenarios (`postgres bm25 ranking fusion`: 2 hits with identical scores; `quantum chromodynamics lattice`: 0 hits) were also identical apart from `scores`. Section 9's AC-100620-03 row now leads with that comparison and keeps the post-change determinism tests as the second half. The absolute values differ from the round-1 adversary's run only because a fresh store was seeded (the store-to-store `admission_score` variance recorded as issue #29); inside one store the two binaries agree exactly, which is what T100620-05 asks."
    },
    {
      "id": "F-03",
      "severity": "low",
      "title": "The required Discovery Output reports post-change (and one wrong) test counts as \"before the change\", and quotes a coverage total that does not match the gate output",
      "evidence": "phase doc section 4 `Discovery Output (implementation time, 2026-09-26)`: \"**Existing test coverage:** 154 `clio-retrieve` tests, 346 `clio-mcp` tests, 64 `clio-lib` read tests before the change\". Measured: `git grep -c '#\\[test\\]' HEAD -- 'crates/clio-retrieve/**'` totals 150; the worktree totals 162; the staged diff adds exactly 12. For clio-mcp, HEAD totals 343, the worktree 346, the staged diff adds 3. So 154 is neither the pre-change nor the post-change count, and 346 is the post-change count. The run log repeats the error at the intermediate step: `private/clio-private/runs/phase-100620/developer-task-r1.log` line \"core verify: ... `cargo test -p clio-retrieve --locked` 154 passed\" (that step added 1 test to a 150-test baseline, so 151). Separately, AC-100620-07 quotes `TOTAL lines 97.87%`; my own `make coverage` on the same tree prints `coverage-guard: TOTAL lines 97.88% functions 98.70%` and the raw JSON is 48422/49472 = 97.8776%.",
      "requirement_ref": "phase doc section 4 \"Discovery Output\" (must report existing test coverage) and section 8 \"Verification Rule\"",
      "recommendation": "Correct the two pre-change counts to 150 (`clio-retrieve`) and 343 (`clio-mcp`), or relabel 162/346 as post-change counts. Re-quote the coverage total from the guard's own output so the number in the phase file is the one the gate printed.",
      "status": "resolved",
      "resolution": "RESOLVED by correcting the counts and re-quoting the coverage total from the gate's own output. Section 4 Discovery Output now reports the measured pre-change counts \u2014 150 `clio-retrieve`, 343 `clio-mcp`, 63 `clio-lib` read tests, from `git grep -c '#[test]' HEAD -- <paths>` on `9a0876c` \u2014 and the post-change counts separately with their measurement method: 162 / 348 / 65, counted with `git grep --untracked -c '#[test]'` and matching the suites that run them (`cargo test -p clio-retrieve --locked` 162 passed, `cargo test -p clio-mcp --locked` 348 passed, `cargo test -p clio --locked cli_read` 65 passed). The coverage total is now quoted as the guard prints it: final `coverage-guard: TOTAL lines 97.88% functions 98.70%` (raw 48422/49472 = 97.8796%) and baseline guard-printed 97.87% (raw 97.8746%); AC-100620-07 and Completion Evidence carry the guard output instead of the raw JSON percentage."
    },
    {
      "id": "F-04",
      "severity": "low",
      "title": "No binding-level or real-binary evidence ever shows a populated `semantic`, so the AC-100620-06 parity claim is only exercised on the null case",
      "evidence": "Every binding test deliberately runs with no embedder, and the tests say so. `crates/clio-mcp/src/read_scores_tests.rs:30-31` (\"no embedder is resolved, so the dense arm is off and `semantic` must be `null`\") and `:194` (`assert!(!state.pipeline.dense_available, \"no embedder configured: dense arm is off\")`). `crates/clio-lib/src/cli_read_tests.rs:162` asserts `scores[\"semantic\"].is_null(), \"no embedder: {hit}\"`. The developer's real-binary evidence quoted under AC-100620-02 is `\"scores\":{\"final\":0.0118...,\"keyword\":1e-6,\"reranker\":null,\"semantic\":null}`. My own pre/post binary run also produced `\"semantic\":null` for every hit. `semantic` is populated only in the in-process `clio-retrieve` tests (crates/clio-retrieve/src/stage_score_tests.rs:171 and :207-211). AC-100620-06's evidence therefore says \"exact `semantic`/`keyword`\" parity while `semantic` is null on both sides. Section 9 `Verification limits` discloses only the Postgres gap, not this one.",
      "requirement_ref": "phase doc section 8 T100620-01 and T100620-06; section 9 AC-100620-06",
      "recommendation": "Add one binding test (or a real-binary run) with a stub embedder so a populated `semantic` is observed in the MCP and CLI JSON, and add this limit to `Verification limits`. The transform itself is already pinned by unit tests and by crates/clio-retrieve/src/stage_score_tests.rs:207-211 against a real anti-parallel vector fixture, so the residual risk is low.",
      "status": "resolved",
      "resolution": "RESOLVED by binding-level tests on both surfaces plus a live Postgres payload check, and by narrowing the written claim. MCP: `dense_arm_populates_semantic_in_mcp_payload_with_parity` (crates/clio-mcp/src/read_scores_tests.rs) opens the state with an `embed` block pointing at a local fake TEI double, drains the queued job (`report.applied == 1`), retrieves, and asserts `dense_rank` numeric, `scores.semantic` a JSON number in [0,1] and not null, `keyword` numeric, `reranker` null, `final == score`, the exact key set, and in-process parity on the same `semantic`; `cargo test -p clio-mcp --locked read_scores` \u2192 `test result: ok. 4 passed; 0 failed`, repeated five times and with `--test-threads=1` (stable). CLI: `recall_json_semantic_is_populated_with_live_embedder` (crates/clio-lib/src/cli_read_scores_tests.rs) drives the real `recall --output json` verb after `drain_index()` applied the item's job and asserts a populated numeric `semantic`, numeric `dense_rank`, `final == score`, `reranker` null, `keyword` numeric, and MCP parity on the same value; `cargo test -p clio --locked cli_read` \u2192 `65 passed; 0 failed`. Postgres: `postgres_retrieve_scores_observes_ts_rank_cd_keyword_end_to_end` (crates/clio-mcp/tests/mcp_read_scores_postgres_test.rs) observed the live `ts_rank_cd` value in the payload \u2014 `keyword=0.10000000149011612 backend=0.10000000149011612` \u2014 with `semantic`/`reranker` null and `final == score`, and returns early when `DATABASE_URL` is not Postgres (verified with `env -u DATABASE_URL`). Section 9 Verification limits now discloses that the populated `semantic` is observed with a deterministic stub embedder (one fixed 384-dim unit vector), not with a real embedding model."
    },
    {
      "id": "F-05",
      "severity": "low",
      "title": "`finalize.rs` and `stage_score.rs` both claim ownership of the `scores` object",
      "evidence": "`crates/clio-retrieve/src/finalize.rs:14` under `## Owns`: \"The per-hit `scores` object attached to each `ScoredHit`\". `crates/clio-retrieve/src/stage_score.rs:11` under `## Owns`: \"`HitScores`, the nested `scores` object serialized on every `ScoredHit`\", while its `## Does not own` (line 17) excludes \"Final hit assembly (`finalize`)\". Two modules claim the same artifact.",
      "requirement_ref": "AGENTS.md \"File Header\" (\"The responsibility and ownership statements MUST accurately describe the actual module\"); phase doc section 5 Task 3 (\"Remove the two-parallel-fields smell: one structure holds per-arm rank and per-arm score provenance\")",
      "recommendation": "Split the claim: `stage_score.rs` owns the `HitScores` type, its field contract, and the dense transform; `finalize.rs` owns populating the fields from the per-arm signals. Say so in both headers so the next reader has one place to look.",
      "status": "resolved",
      "resolution": "RESOLVED. crates/clio-retrieve/src/stage_score.rs now owns \"`HitScores`, the `scores` object type and its field contract\" and its `## Does not own` names `finalize` for populating the fields; crates/clio-retrieve/src/finalize.rs now owns \"Populating each `ScoredHit`'s `scores` fields from the per-arm signals\" and its `## Does not own` names the type, field contract, and transform as `stage_score`'s. The follow-up audit the finding asked for was done: every `## Owns` / `## Does not own` block under crates/clio-retrieve/src was scanned and `scores` was the only artifact claimed twice \u2014 the hybrid / fusion / hybrid_rank / hybrid_util / dedup / finalize / rerank / types claims are disjoint."
    },
    {
      "id": "F-06",
      "severity": "low",
      "title": "The same doc still carries a second, now-stale output-stability claim that this phase invalidates",
      "evidence": "`docs/recall-scope-and-dedup.md:31`: \"`balanced` is the neutral default: with no scope preset and no `prefer_consolidated`, it applies no cap, so an unchanged bank keeps its pre-existing recall output.\" After this phase an unchanged bank's recall output is not its pre-existing one: every hit now carries `scores`. The developer corrected the stricter claim on line 35 (\"keeps its pre-scope shape apart from the always-present per-hit `scores` object\") but left line 31 as is.",
      "requirement_ref": "phase doc section 5 Task 2 Required Changes item 5 (\"Update `docs/recall-scope-and-dedup.md` where it promises a byte-for-byte payload for plain calls\"); section 9 Definition of Done \"Documentation is updated where required\"",
      "recommendation": "Qualify line 31 the same way as line 35, for example \"...keeps its pre-existing recall output apart from the always-present per-hit `scores` object\". The claim is about the dedup cap, so only the payload wording needs the qualifier.",
      "status": "resolved",
      "resolution": "RESOLVED. docs/recall-scope-and-dedup.md:31 now reads \"... keeps its pre-existing recall output apart from the always-present per-hit `scores` object (see [Per-hit `scores`](recall-scores.md))\", matching line 35's qualifier. The operator note was added as docs/recall-scores.md (43 lines): what `final` / `reranker` / `semantic` / `keyword` mean, the `null` rules (\"a missing arm value is `null`, never `0`\"), that `final` is relative and never calibrated, that `keyword` is backend-dependent (SQLite `-bm25(items_fts)` vs Postgres `ts_rank_cd`, not comparable across backends), the determinism caveat (the recency term reads the wall clock; the conformance mask masks `score` and `scores.final`), and where the values are visible (`retrieve` / `recall` / in-process JSON yes; `compose_context` packs text so no; the `explanation` trace and the CLI text breakdown no)."
    },
    {
      "id": "F-07",
      "severity": "low",
      "title": "`mcp_read_conformance.rs` was held at exactly 450 lines by shortening an existing comment, which AGENTS.md discourages, and the file now has zero headroom",
      "evidence": "`crates/clio-mcp/tests/mcp_read_conformance.rs` is 450 lines. The staged diff replaces the two-line comment \"Non-deterministic across runs: wall-clock timing and recency-boosted / floats differ in their last bits even with identical data.\" with the single line \"Non-deterministic across runs: timing and recency-boosted float bits.\" The developer's run log states the reason: \"rustfmt pushed mcp_read_conformance.rs to 451 lines, so shortened the volatile-mask comment by one line to keep the file at exactly 450.\" The same phase also pushed `crates/clio-lib/src/cli_read_tests.rs` to 449 lines. The required edit itself was necessary and correct: `scores.final` mirrors the wall-clock-sensitive `score` and had to join the volatile mask at line 209.",
      "requirement_ref": "AGENTS.md \"Source File Size Constraint\" item 4 (\"Avoid artificial line reduction through compressed formatting, removal of meaningful documentation, or omission of required tests\")",
      "recommendation": "No 450-line violation exists today, so this is not a gate failure. Split the conformance file (for example move `mask_volatile` plus the read conformance cases into two files) so the next edit has headroom, instead of shrinking a comment to buy one line.",
      "status": "resolved",
      "resolution": "RESOLVED by splitting both files instead of buying a line with a compressed comment. crates/clio-mcp/tests/mcp_read_conformance.rs 450 \u2192 177 lines; the three transport tests and their harness moved to crates/clio-mcp/tests/mcp_read_transport_conformance.rs (259) and the shared fixtures to crates/clio-mcp/tests/mcp_read_support/mod.rs (114, with a module-level `#![allow(dead_code)]` so a helper used by only one binary cannot break the build), and the two-line volatile-mask comment is restored verbatim from HEAD with `\"final\"` kept in the 13-entry mask. crates/clio-lib/src/cli_read_tests.rs 449 \u2192 398; the score-object tests moved to crates/clio-lib/src/cli_read_scores_tests.rs (258), declared as a nested `#[path]` child from inside cli_read_tests.rs so the 447-line cli_read.rs stays untouched. No test was weakened: a function-level comparison against the staged baseline shows every moved function byte-identical in both slices (only visibility keywords were added in the shared support module, and `mask_volatile` differs only by the intended `+\"final\"`), and `--list` on both MCP binaries returns the same 10 test names as before the split (7 + 3)."
    }
  ],
  "addressed_issues": [],
  "plan_1hr": [
    "DONE (F-01) - the approval box is unchecked and \u00a712 Final Status is left undecided for the Remedy Approver; the implementer's premature `PASS` is withdrawn in place (see F-01 resolution). No verifier and no approval are claimed anywhere.",
    "DONE (F-02) - the pre/post binary comparison was reproduced by me and is now the AC-100620-03 evidence, with the harness and its JSON kept in the run directory (`remediator-compare-pre-post.py`, `remediator-pre-post-compare.json`, `failures: []`).",
    "DONE (F-03) - Discovery Output now reports 150 / 343 / 63 as the measured pre-change counts and 162 / 348 / 65 as the post-change counts; the coverage total is quoted as the guard prints it (final 97.88% / 98.70%, baseline 97.87% / 98.70%).",
    "DONE (F-04) - the `Verification limits` line was added and re-worded to disclose the stub embedder, and the finding itself was closed for real with populated-`semantic` binding tests plus a live Postgres payload test (see F-04 resolution)."
  ],
  "plan_unlimited": [
    {
      "item": "Add a stub-embedder binding test in clio-mcp and a clio-lib CLI test so a populated `semantic` is observed in the MCP and CLI JSON, closing the T100620-06 gap for real (F-04).",
      "disposition": "DONE - `dense_arm_populates_semantic_in_mcp_payload_with_parity` (crates/clio-mcp/src/read_scores_tests.rs, hermetic SQLite + a bounded local fake TEI double, drain applied 1 job) and `recall_json_semantic_is_populated_with_live_embedder` (crates/clio-lib/src/cli_read_scores_tests.rs, the real `recall --output json` verb) both observe a populated, numeric `semantic` and cross-check it against the other binding. Runs: `cargo test -p clio-mcp --locked read_scores` -> 4 passed / 0 failed (stable across 5 repeats and `--test-threads=1`); `cargo test -p clio --locked cli_read` -> 65 passed / 0 failed. Residual limit kept honest in \u00a79: the embedder is a deterministic stub, not a real model."
    },
    {
      "item": "Split `crates/clio-mcp/tests/mcp_read_conformance.rs` (450 lines, zero headroom) and `crates/clio-lib/src/cli_read_tests.rs` (449 lines) so the next change does not have to compress a comment to fit (F-07).",
      "disposition": "DONE - 450 -> 177 (+259 +114) in clio-mcp and 449 -> 398 (+258) in clio-lib, with the restored two-line mask comment, no test removed or weakened, and the same 10 conformance test names across the two MCP binaries (`--list` compared before/after). Every touched file is far under the 450-line cap (largest 430)."
    },
    {
      "item": "Rewrite the two overlapping `## Owns` entries so `stage_score.rs` owns the `HitScores` contract and transform and `finalize.rs` owns populating the fields, then check the rest of clio-retrieve for the same overlap (F-05).",
      "disposition": "DONE - the two headers now split the claim exactly that way (crates/clio-retrieve/src/stage_score.rs, crates/clio-retrieve/src/finalize.rs), and the requested audit of the rest of clio-retrieve found no other artifact claimed by two modules."
    },
    {
      "item": "Qualify `docs/recall-scope-and-dedup.md:31` alongside line 35, and add a short operator-facing note describing the `scores` object and its `null` rules (F-06).",
      "disposition": "DONE - line 31 is qualified and docs/recall-scores.md (43 lines) documents the four values, the `null` rules, the relative/not-calibrated rule, the backend-dependent `keyword` scale, determinism, and where the object is visible."
    },
    {
      "item": "Add a Postgres-backed end-to-end check of the `scores` payload so the `ts_rank_cd` `keyword` scale is observed, not only documented; the developer disclosed this limit but no phase currently owns closing it.",
      "disposition": "DONE - crates/clio-mcp/tests/mcp_read_scores_postgres_test.rs (243 lines; gated on a Postgres `DATABASE_URL`, private `ScopedSchema` + `lock_pg_test`, unique ids) asserts the live payload: `keyword=0.10000000149011612` equal to the `clio_index::lexical_search` value and non-negative, `semantic`/`reranker` null, `final == score`, numbers-or-null only. It ran and passed against the compose Postgres for this round, so no separate phase ownership is needed. Residual limit (now in \u00a79 Verification limits): one query shape only, and cross-backend comparability is still not claimed or tested."
    }
  ],
  "remediated_by": "Command Code (DeepSeek V4 Flash (latest) Max), remediator r1"
}
