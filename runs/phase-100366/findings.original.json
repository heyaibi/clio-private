{
  "run": "r1",
  "phase": "100366",
  "subject": "Phase 100366: Full CLI Core Plumbing and Retrieval Read Surface",
  "verdict": "Adequate",
  "verdict_justification": "The developer successfully delivered the shared argument parser (cli_args), output layer (cli_output), error contract (cli_error), MCP-derived help catalog (cli_help), and the flagship read commands recall, get/show, inspect, and stats routed in-process via McpHandler without drift. Test suite passes (make test exit 0), clippy is clean (make lint exit 0), and workspace coverage is 97.93% lines / 98.90% functions across 286 files with all changed files >=95%. However, five findings were identified: subcommand --help is completely unhandled by read commands, unknown command error reporting ignores explicit --output flags, split_leading_globals misparses consecutive value flags, parse_failure overrides explicit --output formatting, and a minor grammar error in get argument requirements.",
  "metadata": {
    "generated": "2026-09-23",
    "applies_to": "Phase 100366 staged diff",
    "source": "crates/clio-lib/src/{cli_args,cli_error,cli_help,cli_output,cli_read,cli_read_render,main}.rs"
  },
  "standard": [
    "Phase 100366 specification (roadmap/phase-100366-full-cli-core-and-retrieval-reads.md)",
    "requirement.md \u00a74.9.2 (read bindings, tool schema catalog, exit codes)",
    "AGENTS.md file size cap (<=450 lines) and per-file coverage floors (>=90%)",
    "Workspace gates: make test, make lint, make coverage"
  ],
  "findings": [
    {
      "id": "F-01",
      "severity": "high",
      "title": "Subcommand `--help` / `-h` is unhandled by the read surface, executing commands or erroring on missing positionals",
      "evidence": "crates/clio-lib/src/cli_args.rs:118-121 parses `-h` and `--help` into `parsed.flags` under `help`, but `crates/clio-lib/src/cli_read.rs:103-124` never inspects `parsed.has(\"help\")`. Executing `target/debug/clio stats --help --db sqlite::memory:` executes `stats` and returns data payload instead of help text; executing `target/debug/clio recall --help` fails with exit 2 `{\"code\":\"usage\",\"hint\":\"usage: clio recall QUERY\",\"message\":\"`recall` requires a QUERY argument\",\"ok\":false}`.",
      "requirement_ref": "Phase 100366 \u00a71 Expected Outcome, \u00a75 Task 1 & Task 2, AC-100366-01",
      "recommendation": "In `cli_read::run_parsed` (or `run_verb`), check `if parsed.has(\"help\")`: render the subcommand usage text and exit with `ExitCode::SUCCESS`."
    },
    {
      "id": "F-02",
      "severity": "medium",
      "title": "Unknown-command error reporting ignores explicit `--output json` on TTY and `--output text` when piped",
      "evidence": "crates/clio-lib/src/main.rs:94-98 calls `cli_output::report_error(&error, cli_output::stdout_is_tty(), cli_output::no_color_requested())`, where `cli_output.rs:114-119` determines mode strictly by terminal attachment without inspecting `--output`. Live output: on a terminal, `clio recal --output json` outputs colored human text `error: unknown command `recal`` instead of `{ok:false, code, message, hint}`; when piped, `clio recal --output text 2>&1 | cat` outputs the JSON envelope instead of human text.",
      "requirement_ref": "Phase 100366 \u00a71, \u00a72 In Scope, \u00a75 Task 1, AC-100366-02, AC-100366-03",
      "recommendation": "In `main.rs` unknown command handler (or in `cli_output::report_error`), inspect `trailing` or `collected` for `--output json` / `--output text` so an explicit `--output` overrides TTY detection."
    },
    {
      "id": "F-03",
      "severity": "medium",
      "title": "`split_leading_globals` consumes subsequent flag token as value when a leading value flag lacks an argument",
      "evidence": "crates/clio-lib/src/cli_args.rs:187-195 pushes `args.get(index)` as the flag value without checking `!value.starts_with(\"--\")`. Live output: `target/debug/clio --output --db sqlite::memory: recall q` consumes `--db` as the value of `--output` and treats `sqlite::memory:` as the command verb, exiting 2 with `error: unknown command `sqlite::memory:`` instead of reporting that `--output` requires a value.",
      "requirement_ref": "Phase 100366 \u00a75 Task 1, AC-100366-05",
      "recommendation": "In `split_leading_globals`, verify that `!value.starts_with(\"--\")` before consuming `value` for a value-taking flag."
    },
    {
      "id": "F-04",
      "severity": "low",
      "title": "`parse_failure` in `cli_read` ignores explicit `--output text` when piped and `--output json` on TTY",
      "evidence": "crates/clio-lib/src/cli_read.rs:401-407 hardcodes `mode = if io.is_tty { OutputMode::Text } else { OutputMode::Json };`. Live output: `target/debug/clio recall q --output text --limt 2 2>&1 | cat` outputs JSON `{\"code\":\"usage\",\"hint\":\"did you mean `--limit`?\",\"message\":\"unknown flag `--limt`\",\"ok\":false}` despite explicit `--output text`.",
      "requirement_ref": "Phase 100366 \u00a71, \u00a75 Task 1, AC-100366-02",
      "recommendation": "In `parse_failure`, scan `args` for valid `--output json` / `--output text` (or `--output=...`) before defaulting to TTY mode."
    },
    {
      "id": "F-05",
      "severity": "low",
      "title": "Grammar defect in missing positional argument error message",
      "evidence": "crates/clio-lib/src/cli_read.rs:235 formats error as `format!(\"`{verb}` requires a {label} argument\")`. When label is \"ID\", `target/debug/clio get` produces `message: \"`get` requires a ID argument\"` with incorrect indefinite article.",
      "requirement_ref": "Phase 100366 \u00a75 Task 2",
      "recommendation": "Adjust phrasing to `format!(\"`{verb}` requires an {label} argument\")` or choose the article dynamically based on vowel start."
    }
  ],
  "plan_1hr": [
    "In crates/clio-lib/src/cli_read.rs, check if parsed.has(\"help\") in run_parsed. Render the subcommand usage text and return ExitCode::SUCCESS.",
    "In crates/clio-lib/src/main.rs, check trailing tokens for `--output json` / `--output text` when reporting unknown command errors so explicit --output overrides TTY detection.",
    "In crates/clio-lib/src/cli_args.rs:split_leading_globals, guard args.get(index) with `!value.starts_with(\"--\")` before consuming it as a value for a value-taking flag.",
    "In crates/clio-lib/src/cli_read.rs:parse_failure, inspect args for explicit `--output` flags before falling back to TTY detection.",
    "In crates/clio-lib/src/cli_read.rs:one_positional, update article formatting to \"an {label}\" where appropriate."
  ],
  "plan_unlimited": [
    "Add dedicated unit and integration tests covering subcommand-level `--help` and `-h` across all read subcommands (`recall`, `get`, `show`, `inspect`, `stats`).",
    "Centralize global flag pre-parsing across top-level dispatch so output mode, color, and database resolution behave identically across all CLI commands and error paths."
  ]
}