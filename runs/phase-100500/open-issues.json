{
  "issues": [
    {
      "body": "### Summary\nThe CLI argument parser in `crates/clio-lib/src/cli_args.rs` does not recognize the standard POSIX `--` end-of-options delimiter and treats any token starting with `-` (or `--`) as a flag. As a result, attempting to use `--` to delimit options from positional arguments fails with `unknown flag '--'`, and positional arguments starting with a hyphen (such as markdown lists, bullet points, or negative numbers) cannot be supplied.\n\nFound incidentally during benchmark adapter review; was not fixed as it is outside the current task scope.\n\n### Steps to Reproduce\n1. Attempt to pass `--` before a positional argument:\n   ```bash\n   cargo run -q -- remember --category output_constraint --bank test --actor user --output json -- \"- note\"\n   ```\n2. Or attempt to pass a string starting with `-` directly:\n   ```bash\n   cargo run -q -- remember \"- note\" --category output_constraint --bank test --actor user --output json\n   ```\n3. Or attempt to recall a string starting with `-`:\n   ```bash\n   cargo run -q -- recall \"- query\" --output json\n   ```\n\n### Expected Behavior\n- When `--` is encountered, flag parsing should terminate and all subsequent tokens should be appended to `positionals`.\n- Tokens following `--` (even if starting with `-`) should be treated as positional arguments.\n\n### Actual Behavior\n- Passing `--` produces:\n  ```json\n  {\"code\":\"usage\",\"message\":\"unknown flag `--`\",\"ok\":false}\n  ```\n- Passing a hyphen-prefixed positional directly produces:\n  ```json\n  {\"code\":\"usage\",\"message\":\"unknown flag `- note`\",\"ok\":false}\n  ```\n\n### Evidence\nIn `crates/clio-lib/src/cli_args.rs`:\n```rust\n        let Some(body) = token.strip_prefix(\"--\") else {\n            if token.starts_with('-') && token != \"-\" {\n                return Err(unknown_flag(&token, spec));\n            }\n            parsed.positionals.push(token);\n            continue;\n        };\n```\nWhen `token == \"--\"`, `token.strip_prefix(\"--\")` yields `\"\"`. The name lookup fails against `spec.bool_flags()` and `spec.value_flags()`, and the parser invokes `unknown_flag(\"--\", spec)`, returning exit code 2.\nFurthermore, line 123 rejects any token where `token.starts_with('-') && token != \"-\"` as an unknown flag, providing no mechanism to pass hyphenated text.\n\n### Impact\nCLI users, scripts, and adapters cannot store or recall propositions or queries beginning with `-` (such as markdown bullet lists or diff excerpts) through the CLI.\n\n### Proposed Fix\nIn `crates/clio-lib/src/cli_args.rs`:\n1. Check if `token == \"--\"`. If so, append `tokens[index..]` directly to `parsed.positionals` and break out of the parsing loop.\n2. In `split_leading_globals`, also stop splitting when `--` is encountered.\n",
      "comment_count": 0,
      "comments": [],
      "comments_truncated": false,
      "labels": [],
      "number": 15,
      "state": "open",
      "state_reason": null,
      "title": "CLI argument parser rejects end-of-options delimiter '--' and treats hyphen-prefixed positional arguments as unknown flags",
      "triage_digest": "5f620b32d26bf1863596ad9e9071d09e63a0a40478d7ec86fa7833aa0a8431ba",
      "updated_at": "2026-09-24T18:25:52Z",
      "url": "https://github.com/heyaibi/clio/issues/15"
    },
    {
      "body": "## Symptom\n\nAn index job that has failed `DEFAULT_MAX_ATTEMPTS` (10) times is counted as `skipped` on every\nlater sweep and never leaves the queue:\n\n```rust\nif job.attempts >= self.max_attempts {\n    // Exhausted: leave pending (status shows the error); rebuild or\n    // a manual no-cutoff sweep can still clear it.\n    report.skipped += 1;\n    continue;\n}\n```\n\nThe row keeps its old `last_attempt_at`, so the due query returns it on every sweep, and that query\nis `ORDER BY queued_at` with a bounded batch. An exhausted row therefore permanently occupies one\nof the 32 batch slots, ahead of every newer row (`queued_at` is older). With 32 or more exhausted\nrows, `drain_index()` only ever returns those rows (`skipped` each time) and newly stored items are\nnever indexed until an operator rebuilds. The reported counts make it look like the sweep is\nworking: `ProcessReport { applied: 0, skipped: 32, failed: 0 }`.\n\nThe comment's second claim is also wrong: a forced sweep does not clear an exhausted row.\n`process_pending_forced` takes the same branch and only re-counts it as `skipped`. What does clear\nit is a rebuild (which calls `finish_index_job` per item) or the item vanishing (`cleanup_orphans`).\n\n## Where\n\n- `crates/clio-index/src/worker.rs:201-206` (the exhausted branch) and `:184-195`\n  (`process_pending` / `process_pending_forced`)\n- `crates/clio-store/src/postgres_index.rs:241-267` (due query: `ORDER BY queued_at LIMIT $2`,\n  a row with an old `last_attempt_at` is always due)\n- `crates/clio-mcp/src/index_drain.rs:39` (`INDEX_MAX_BATCH = 32`)\n- Recovery path: `crates/clio-index/src/rebuild.rs:138` and `:156` (`finish_index_job` per item),\n  `:106` (`cleanup_orphans`)\n\n## Reproduction\n\nUnit level, extending the harness that already exists in `crates/clio-index/src/worker_tests.rs`\n(`FailEmbedder`, `t02_sidecar_down_pending_remains_and_rebuild_recovers`):\n\n1. Create one item with a store, build a coordinator over a failing embedder, and sweep until\n   `attempts` reaches `DEFAULT_MAX_ATTEMPTS` (bypass the 30s window with\n   `process_pending_forced`).\n2. Sweep again: the report counts the job as `skipped`, and\n   `pending_index_jobs(limit, None)` still returns the row with `attempts = 10`.\n3. Confirm the slot cost: seed 32 such rows plus one fresh item, then sweep with\n   `INDEX_MAX_BATCH`; the fresh item is not in the batch.\n\nAgainst Compose Postgres (`make compose up mac`), the same state is visible with\n`SELECT attempts, last_attempt_at FROM index_pending ORDER BY queued_at;` after a dead\n`embed.url` run.\n\n## Impact\n\nA long embed or rerank outage that leaves 32 or more rows exhausted silently stops new indexing\n(no dense or lexical rows are written for new items) even though the provider has recovered, and\nthe runtime never recovers on its own. The rows stay visible as `retry_jobs` in\n`maintenance_status`, so the state is diagnosable, but the batch is blocked until someone runs a\nrebuild.\n\n## Proposed fix\n\nDecide the exhaustion policy explicitly and make the row stop consuming a slot. Either:\n\n- Delete the row once it is exhausted (`finish_index_job`) and rely on `index_coverage` /\n  `last_error` for visibility; or\n- Keep the row but stop returning `attempts >= max_attempts` rows from `pending_index_jobs`, so it\n  cannot sit at the head of the batch.\n\nEither way, correct the `worker.rs` comment: a forced sweep does not clear an exhausted row.\n\nFound while fixing #9. Recorded here; not fixed there.\n",
      "comment_count": 1,
      "comments": [],
      "comments_truncated": true,
      "labels": [],
      "number": 14,
      "state": "open",
      "state_reason": null,
      "title": "Exhausted index jobs never leave the outbox and can starve new jobs",
      "triage_digest": "ba0198f30d3766709e9f4a6a11ebb62beed100d165fc026d2e5e30ebbc8d15e3",
      "updated_at": "2026-09-24T16:25:13Z",
      "url": "https://github.com/heyaibi/clio/issues/14"
    },
    {
      "body": "## Symptom\n\n`clio_store::lock_pg_test()` is documented as serializing \"every test that touches the\nshared `DATABASE_URL` / Compose Postgres instance\" \"across crates\", but it is a\nprocess-local `std::sync::Mutex`. `make test` runs `cargo test --locked --workspace`, which\nruns each test binary as its own process in parallel, so the Postgres suites of clio-store,\nclio-retrieve, clio-history, clio-write, and clio-mcp touch the same database at the same\ntime. The guard only orders tests inside one binary.\n\nTwo consequences are visible on a normal full run:\n\n1. **Suites drop `public.items` while other binaries are mid-test.**\n   - `crates/clio-store/src/memory_item_tests.rs:305-330` (`postgres_resets_stale_class_schema`)\n     opens the shared URL, then runs `DROP TABLE IF EXISTS items CASCADE; CREATE TABLE items (...)`.\n   - `crates/clio-store/src/schema_reshape.rs:372-396` (`pg_reshape_guard_preserves_rows_then_wipes_when_empty`)\n     does the same on `public.items`.\n   Any other test process reading or writing items during that window sees a missing or legacy\n   table. After a full run `SELECT count(*) FROM items` on the Compose database is 0.\n\n2. **The queue outlives its items.** `index_pending` has no foreign key to `items` (that is why\n   `IndexStore::cleanup_orphans` exists), so when one of those suites replaces `items`, every\n   queued row becomes an orphan. A run leaves rows behind:\n   `SELECT bank_id, item_id, attempts FROM index_pending;` returns rows for banks whose items no\n   longer exist (for example the `pg-sg-*` subject-guard banks).\n\n## Where\n\n- `crates/clio-store/src/pg_lock.rs:6-11` (doc claim) and `:26` (`pub static PG_TEST_LOCK: Mutex<()>`)\n- `Makefile:107` (`cargo test --locked --workspace`)\n- `crates/clio-store/src/memory_item_tests.rs:305`, `crates/clio-store/src/schema_reshape.rs:372`\n- `crates/clio-store/src/postgres_index.rs:349-367` (`cleanup_orphans`, the only sweep that removes\n  queue rows for vanished items)\n\n## Reproduction\n\n1. `make compose up mac`\n2. Run two shared-Postgres suites from different crates at the same time, for example\n   `cargo test --locked -p clio-store --lib postgres_resets_stale_class_schema` and, concurrently,\n   `cargo test --locked -p clio-mcp --lib index_drain_hardening`. Each process takes its own\n   in-process guard, so both proceed.\n3. While they run: `docker compose -f compose.yml exec -T postgres psql -U clio -d clio -c \"SELECT count(*) FROM items;\" -c \"SELECT count(*) FROM index_pending;\"`.\n4. After any full `make test`: `items` is empty while `index_pending` holds rows pointing at\n   items that no longer exist.\n\nObserved instance: issue #9 failed because the clio-mcp drain asserted a store-global report\nwhile another binary's item was live in the same database; the item was sealed under that\nprocess's `LocalDevKms` key, so the drain recorded\n`forbidden: no DEK for subject ...` as a job failure.\n\n## Impact\n\nTests that read anything store-global (counts, drain reports, whole-store sweeps, coverage) can\nobserve or hit another suite's rows, and a `DROP TABLE` in one process can break an unrelated\ntest in another. Failures are timing dependent, so they surface as flakes rather than as a\nconsistent red suite. Leftover rows also mean the shared database slowly accumulates state that\nno test owns.\n\n## Proposed fix\n\nPick one and apply it consistently:\n\n- Make the guard cross-process (a Postgres advisory lock on the shared database, or a lock file\n  under `target/`), and update the `pg_lock` doc to state the scope it actually provides; or\n- Move the destructive suites (`memory_item_tests::postgres_resets_stale_class_schema`,\n  `schema_reshape::pg_reshape_guard_preserves_rows_then_wipes_when_empty`) onto a private schema\n  via `ScopedSchema` (`crates/clio-store/src/pg_scoped_schema.rs`), which is what #9 did for the\n  clio-mcp drain test.\n\nFound while fixing #9. Recorded here; not fixed there.\n",
      "comment_count": 1,
      "comments": [],
      "comments_truncated": true,
      "labels": [],
      "number": 13,
      "state": "open",
      "state_reason": null,
      "title": "Shared-Postgres test lock does not serialize across test binaries (suites drop public.items mid-run)",
      "triage_digest": "fe187786acfd7a3f07ef8c91ac3037a9eba04b36048b146440ccb1de9b18a825",
      "updated_at": "2026-09-24T16:25:10Z",
      "url": "https://github.com/heyaibi/clio/issues/13"
    },
    {
      "body": "## Symptom\nPer-item crypto cost grows with the number of stored items, so any path that decrypts a bounded set of rows still slows down as a bank grows.\n\n## Where\n- `crates/clio-store/src/dek.rs:263` `LocalDevKms::decrypt` calls `self.refresh(&mut slots)` before every lookup.\n- `crates/clio-store/src/dek.rs:184` `refresh` does `*slots = load_slots(path)?` \u2014 reads and parses the entire key JSON file each call.\n- `ensure_dek` (`dek.rs:223`) and `encrypt` (`dek.rs:237`) do the same.\n- The file grows because the CLI creates one DEK per item: `build_item` sets `subject_id` to the item id when the caller omits one, so every `remember` adds a new subject slot.\n\n## Impact\nEach `get_memory_item` decrypt is O(key-file size). Measured on a file-backed SQLite DB with the default profile:\n\n- `clio admit` (bounded scan of 64 rows) at 300 items: ~0.29s; at 600 items: ~0.40s; the delta tracks the key file doubling (33KB -> 67KB).\n- Before the #6 fix the same path decrypted the whole bank twice; that was ~2.2s at 588 items. The #6 fix bounded the decrypt count to 64, but each decrypt is still O(n), so the write path remains linear in bank size.\n\n## Proposed fix\n- Keep the slot map in memory and only reload on a miss, on a version/`mtime` change, or on an explicit invalidation; do not reload on every call.\n- If cross-process freshness is required, gate the reload on file `mtime`/size rather than unconditional reparse.\n- Alternatively, persist DEKs per subject so the file does not grow with item count (for example a fixed per-bank DEK when the caller omits `subject_id`).\n\nDiscovered while fixing #6; not changed there to keep that fix scoped.",
      "comment_count": 0,
      "comments": [],
      "comments_truncated": false,
      "labels": [],
      "number": 12,
      "state": "open",
      "state_reason": null,
      "title": "LocalDevKms reloads and reparses the whole key file on every decrypt/encrypt",
      "triage_digest": "ba526777cd96caa005719420da0ff4d69965603dd8114e58c92a75085ff067ba",
      "updated_at": "2026-09-24T09:24:18Z",
      "url": "https://github.com/heyaibi/clio/issues/12"
    }
  ],
  "repository": "heyaibi/clio"
}
