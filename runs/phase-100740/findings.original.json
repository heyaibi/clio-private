{
  "run": "phase-100740 (adversary r1)",
  "phase": "100740",
  "findings": [
    {
      "id": "F-01",
      "severity": "low",
      "title": "The drift guard only sees serialized hit keys, so a new ScoredHit field marked #[serde(skip)] can be left unclassified with no test failure",
      "evidence": "read_explain_guard_tests.rs:199 builds the sweep input as `keys(&serde_json::to_value(&hit).expect(\"serialize full hit\"))` and read_explain_guard_tests.rs:213-224 iterates only that set, so a field that serde never emits is invisible to the guard. Verified by independent mutation in an isolated git worktree: adding `drift_probe: Option<String>` with `#[serde(skip)]` to crates/clio-retrieve/src/types.rs, naming it `drift_probe: _` in the exhaustive pattern at read_explain.rs:114-125, and setting it in the `full_hit()` fixture left `cargo test -p clio-mcp --locked --lib read_explain` at '6 passed; 0 failed'. The same field WITHOUT `#[serde(skip)]` and left unclassified produced 'test result: FAILED. 5 passed; 1 failed' with 'hit key `drift_probe` is unclassified: add it to TRACE_HIT_KEYS if it belongs in the trace, or to an exclusion set with a reason'. Practical impact is small: a serde-skipped field cannot reach the wire, so it cannot leak content into the trace. What drifts is the declared classification record, and read_explain.rs:49-51 claims 'production code enforces the same classification at compile time through the exhaustive pattern', which is false: the pattern only forces a field to be *mentioned*, and `newfield: _` satisfies it.",
      "requirement_ref": "phase doc §5 Task 1 'Adding a new hit field either appears in the trace (if PII-safe and listed) or is excluded (if content-derived), by deliberate choice in one place'; §8 T100740-03; §12 'Any future hit field is deliberately classified as trace-visible or content-excluded'; AGENTS.md header/ownership accuracy rule",
      "recommendation": "Sweep struct field names instead of serialized keys: add a `..`-free destructuring inside the guard test that binds every field of `ScoredHit` and of the flattened `RetrieveHit` by name, then assert `classify(name).is_some()` for each bound name. The compiler then makes the name list exhaustive, so a `#[serde(skip)]` field is covered and the test fails on an unclassified field. Separately, correct read_explain.rs:49-51 to say the pattern enforces field-mention (with the test enforcing classification), and add a test that fails if `..` or a wildcard is reintroduced into the `explain_hit` pattern."
    },
    {
      "id": "F-02",
      "severity": "medium",
      "title": "The phase's required end-to-end trace test does not exist, the §9 evidence calls an in-process dispatch the real MCP binding, and the whole §8 Required Tests checklist is left unticked",
      "evidence": "phase doc §8 lists '- [ ] End-to-end tests (real MCP `retrieve` with `explain`)' and phase doc §9 AC-100740-01 describes the wire test as asserting the key set 'through the real MCP `retrieve` binding'. The three wire tests actually call the in-process dispatcher: read_explain_tests.rs:167, read_explain_tests.rs:222 and read_explain_tests.rs:264 all invoke `crate::write_tools::dispatch(&state, \"retrieve\", &json!({... \"explain\": true ...}))` with no JSON-RPC frame, no transport, and no `McpHandler`. The only real-transport `retrieve ... explain:true` call in the repo is the pre-existing fixture at crates/clio-mcp/tests/mcp_read_transport_conformance.rs:44-47 (mirrored over HTTP through the same fixture list), and that test asserts only masked transport parity, never the trace key set. The §8 checklist is still `- [ ]` for all seven categories and the 'Required Test Scenarios' table has no result column, while phase doc §9 and the 'Definition of Done' checkbox 'Required tests pass' both claim the tests pass.",
      "requirement_ref": "phase doc §8 'Required Tests' and 'Required Test Scenarios'; §9 AC-100740-01 evidence; 'Definition of Done' checkbox 'Required tests pass'",
      "recommendation": "Do one of two things and record which. Either add the missing end-to-end assertion: in the stdio/HTTP fixture path in crates/clio-mcp/tests/mcp_read_transport_conformance.rs, assert that the returned `explanation.hits[*]` key set equals the six trace keys and that no entity name appears, so the end-to-end category has its own contract. Or reword the §9 AC-100740-01 evidence to say 'in-process dispatch' and state in the Completion Evidence that the end-to-end category is covered by the pre-existing transport parity fixture, then tick the §8 checkboxes that are genuinely met and leave a reason next to the ones that are not."
    },
    {
      "id": "F-03",
      "severity": "low",
      "title": "A pre-existing test comment now states the opposite of what the code does, and the read_retrieve module header still claims it owns explain shaping",
      "evidence": "crates/clio-mcp/src/read_entities_tests.rs:357-360 documents `entity_match_never_reaches_the_explanation_trace` with 'the trace is a hand-written projection, so the exclusion is deliberate rather than a side effect of omission'. After the staged change the trace comes from the `ExplainHit` view in read_explain.rs:99-127, so the stated reason is false; the exclusion is now enforced by the projection's field list and proven by `every_hit_field_is_classified_and_matches_the_projection`. The assertion itself still holds and still passes, so this is a stale reason, not a behaviour regression. crates/clio-mcp/src/read_retrieve.rs:20 also still says 'Changes to retrieval read binding or explain shaping should normally require changes here', and line 11 still lists '`explain` trace assembly' under Owns, although per-hit shaping moved to read_explain.rs. Only the function-level doc at read_retrieve.rs:115-120 was updated.",
      "requirement_ref": "AGENTS.md 'The responsibility and ownership statements MUST accurately describe the actual module'; phase doc §2 In Scope 'Updating call sites'; phase doc Definition of Done 'Documentation is updated where required'",
      "recommendation": "Update the comment at read_entities_tests.rs:357-360 to name the real mechanism (the `ExplainHit` allowlist plus the classification sweep) and update the read_retrieve.rs module header so Owns says the top-level trace counters and Boundary points per-hit shaping at `crate::read_explain`. Both are comment-only edits; no test change is needed."
    },
    {
      "id": "F-04",
      "severity": "low",
      "title": "The phase file keeps a wrong file:line citation for the conformance volatile mask as a Required Discovery fact, even though the developer run found it wrong",
      "evidence": "private/clio-private/roadmap/phase-100740-explain-trace-projection.md line 46 and line 97 both cite `crates/clio-mcp/tests/mcp_read_conformance.rs:196-228` as the conformance volatile mask, and §4 'Required Discovery' plus §3 'Dependencies' tell the next agent to re-verify against it. That file is 177 lines long and contains no `mask_volatile` at all (grep for 'mask_volatile' in it returns nothing). The real masks are crates/clio-mcp/tests/mcp_read_transport_conformance.rs:146-163 (VOLATILE with 13 keys, including `score` and `metrics`, which is what the phase file describes) and crates/clio-mcp/tests/conformance.rs:327-350 (VOLATILE with 7 keys and no `score`/`metrics`). The developer's own run log records this discovery, but the phase file was not corrected. The same block also says '`read_retrieve.rs` is 271 lines' (line 49) while the file is now 274 lines.",
      "requirement_ref": "phase doc §4 'Required Discovery' (agent MUST re-verify before editing) and §3 'Dependencies' Validation column",
      "recommendation": "Fix the two citations in the phase file to `crates/clio-mcp/tests/mcp_read_transport_conformance.rs:146-163` (and note the separate mask in `crates/clio-mcp/tests/conformance.rs:327-350`), and update the line-49 file-size note to 274. The citation was already wrong at plan time, so any agent that trusts it will look in a file that has no mask."
    },
    {
      "id": "F-05",
      "severity": "low",
      "title": "The new module's doc comment overstates compile-time enforcement and declares a key order that no test checks",
      "evidence": "read_explain.rs:49-51 says 'The three tables below are the declared classification contract. They compile only for tests; production code enforces the same classification at compile time through the exhaustive pattern in `explain_hit`.' The exhaustive pattern at read_explain.rs:114-125 only forces a field to be named; `newfield: _` compiles cleanly and leaves the field unclassified (demonstrated by mutation C in F-01). read_explain.rs:56 describes `TRACE_HIT_KEYS` as 'in wire order', but every comparison treats it as an unordered set (`keys(hit)` returns a `BTreeSet<String>` at read_explain_tests.rs:60-63 and read_explain_guard_tests.rs:103-106; both assertions compare with `set_of(TRACE_HIT_KEYS)` at read_explain_tests.rs:182 and read_explain_guard_tests.rs:225). serde_json's `preserve_order` feature is not enabled anywhere in the workspace, so `Map` is a `BTreeMap` and the emitted key order is alphabetical regardless of the array order, which makes the 'wire order' note both untested and misleading.",
      "requirement_ref": "AGENTS.md file-header rule: ownership statements must accurately describe the actual module",
      "recommendation": "Reword read_explain.rs:49-51 to state what each mechanism really enforces (the pattern forces a field to be mentioned; the test-only sweep forces it to be classified), and change 'in wire order' on read_explain.rs:56 to 'the trace-visible hit keys' since order is neither emitted nor asserted."
    }
  ],
  "addressed_issues": [],
  "plan_1hr": [
    "Comment-only fixes: correct the stale reason at crates/clio-mcp/src/read_entities_tests.rs:357-360 and the Owns/Boundary lines at crates/clio-mcp/src/read_retrieve.rs:11 and :20 (F-03).",
    "Reword read_explain.rs:49-51 and read_explain.rs:56 so the module doc matches what the code enforces (F-05).",
    "Correct the two stale mask citations and the stale line count in the phase file's Design References and Dependencies blocks (F-04).",
    "Resolve the end-to-end test question one way and write it down: either add the `explanation.hits[*]` key-set assertion to the stdio/HTTP fixture in crates/clio-mcp/tests/mcp_read_transport_conformance.rs, or reword the §9 AC-100740-01 evidence to 'in-process dispatch' and record the end-to-end category as covered by the pre-existing transport parity fixture; then tick the §8 checkboxes that are genuinely met and leave a reason beside the ones that are not (F-02)."
  ],
  "plan_unlimited": [
    "Make the drift guard cover non-serialized fields: add a `..`-free destructuring in read_explain_guard_tests.rs that binds every field of `ScoredHit` and of the flattened `RetrieveHit` by name and asserts each name is classified, so `#[serde(skip)]` fields are swept too (F-01).",
    "Add a test that fails if `..` or a wildcard is reintroduced into the `explain_hit` pattern, so the compile-time forcing function cannot be silently disabled (F-01).",
    "Promote the classification tables out of `#[cfg(test)]` into a shipped constant so the declared allowlist is part of the product contract rather than a test artifact, and have the wire key-set assertion read that one constant instead of a test-local copy (F-01, F-02).",
    "Give the end-to-end category its own contract: a fixture case that asserts the frozen trace key set and the absence of entity names over both real transports, not only masked transport parity (F-02).",
    "Introduce a `ScoredHit` constructor so adding a field breaks every construction site through one place instead of relying on the compiler reaching each struct literal (F-01, F-03).",
    "Add a CI check that the phase-file file:line citations still resolve, so stale private design references are caught at authoring time rather than by the next agent (F-04)."
  ]
}
